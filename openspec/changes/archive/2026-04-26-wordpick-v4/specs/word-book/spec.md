## MODIFIED Requirements

### Requirement: 底部 tabbar
The word-book module SHALL NOT be accessible in v4. Only the 词库 tab remains; the 生词本 tab and page are removed.

#### Scenario: 生词本入口已移除
- **WHEN** 用户使用应用
- **THEN** 系统不显示底部 tabbar，只有词库一条导航线
